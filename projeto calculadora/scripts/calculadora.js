//orientação a objetos: estilo de programação. 

//os objetos ficam agrupados em metodos. Quando precisamos chamar a classe, precisamos instanciar o objeto, pois o objeto vai representar a classe. Por que é importante programar com a progamação orientada a objetos ?  Porque é mais pratico , pode-se reutilizar o codigo em outros projetos e a sua abstração é mais facil.

//mvc : model view controller : view : a interface de interação com o usuario. As regras de negocios é o controller. O model é a base de dados.

//chamando a classe calcController:

let calculadora = new CalcController();

//estamos dizendo que a variavel calculadora é uma instancia (copia) da classe CalcController.